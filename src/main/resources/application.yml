spring:
  application:
    name: USER-SERVICE
  data:
    mongodb:
      uri: mongodb+srv://user:1234@cluster0.4emtz3f.mongodb.net/skillDB?retryWrites=true&w=majority&appName=Cluster0
token:
  secret:
    key: hehe
  expirationms: 36000000

server:
  port: 8077

#eureka:
#  instance:
#    prefer-ip-address: true
#    hostname: eureka-0
#  client:
#    registerWithEureka: true
#    fetchRegistry: true
#    service-url:
#      defaultZone: http://eureka-0.eureka.default.svc.cluster.local:8761/eureka

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost